export const API_URL = "https://65f6713541d90c1c5e0ad28f.mockapi.io/api/users";
export const API_URL_TWO =
  // "https://65f6713541d90c1c5e0ad28f.mockapi.io/api/epmloyeedata";
  "http://localhost:8080/users";

  // const handleOnchange = (e) => {
  //   if (e.target.name === "resume") {
  //     const file = e.target.files[0]; // Get the selected
  //     // const cachedURL = URL.createObjectURL(file);
  //     // setUploaddata({ ...uploaddata, resume: cachedURL }); // Update the state with the base64 data URL of the selected image

  //     const reader = new FileReader(); // Create a new file reader
  //     reader.onload = () => {
  //       setUploaddata({ ...uploaddata, resume: reader.result }); // Update the state with the base64 data URL of the selected image
  //     };
  //     reader.readAsDataURL(file); // Read the selected file as a data URL
  //   } else {
  //     setUploaddata({ ...uploaddata, [e.target.name]: e.target.value });
  //   }
  // };

